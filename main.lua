using "system";
using "system.Diagnostics";
using "system.Threading";
using "system.IO";

class "Program" {
	var = "variable";

	Main = function(args)
		local str = f"This is a {var}!";
		Console.WriteLine(str);
	end;
}
